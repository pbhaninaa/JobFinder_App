import React, { useEffect, useState } from "react";
import {
    ScrollView,
    Text,
    TextInput,
    TouchableOpacity,
    View,
    FlatList,
} from "react-native";
import styles from "./Styles";

const CreateProfile = () => {

    return (

        <View style={styles.body}>
            <Text>Create Profile to be implemented</Text>
        </View>
    );
};

export default CreateProfile;
